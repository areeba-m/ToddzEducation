<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <corners android:radius="4dp" />
            <solid android:color="#D7CECE" /> <!-- Lighter shade for pressed state -->
            <stroke android:width="1dp" android:color="#BEB6B6" /> <!-- Optional: Adjusted stroke color for pressed state -->
        </shape>
    </item>
    <!-- Default state -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="4dp" />
            <solid android:color="#F8F3F3" /> <!-- New default color for the button -->
            <stroke android:width="1dp" android:color="#D1C7C7" /> <!-- Optional: Adjusted stroke color for default state -->
        </shape>
    </item>
</selector>